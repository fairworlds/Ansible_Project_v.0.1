---
- name: Test Connection to my servers
  hosts: all
  become: yes

  tasks:
  - name: Ping my servers
    ping: 
  - name: LINUX version 
    debug: var=ansible_os_family   #####ansible all -m setup 